#include<iostream>
#include<string>
#include<map>

using namespace std;

void calcNGramms(const string& str, size_t n, map<string, size_t> * const result) {
    if (str.size() < n) return;
    
    
    
    delete[] c;
}
